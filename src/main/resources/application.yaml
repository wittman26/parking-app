# Basepath to consult the API {{base-url}}{{basepath}}/parking
basepath: /api
spring:
  data:
    # Database properties
    mongodb:
      host: localhost
      port: 27017
      database: parking-lot-management
server:
  port: '9091'

